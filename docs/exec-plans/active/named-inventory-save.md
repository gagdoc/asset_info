# 이름을 지정한 현재 재고 저장

재고 추적 관리 상단에서 이름(1~80자)을 입력하고 현재 재고 저장을 실행한다. 서버에서 최신 추적 품목을 읽어 별도 시트 재고사용자저장V1에 저장한다. 월 마감이나 현재 수량은 변경하지 않는다. 동일 이름은 별도 ID와 시각으로 구분하며 덮어쓰지 않는다.

저장한 재고 보기 링크를 통해 기존 별도 재고 페이지의 이름으로 저장한 재고 목록/상세/엑셀 내보내기를 제공한다. 완결 표식이 없는 저장은 목록에서 제외한다. 0/음수 보존, 미추적 제외, 이후 현재고 변경에도 저장값 불변을 검증한다.
